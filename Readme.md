# 🚀 React ProgressBar Component for Webflow

A lightweight, customizable **React Progress Bar Component** designed to work seamlessly inside **Webflow** using a compiled ES module bundle.

This repository contains:

- A standalone React component (`ProgressBar.jsx`)
- A minimal build system using **esbuild**
- Fully documented setup instructions
- Webflow usage examples
- A compiled bundle ready for embedding into Webflow

---

## 📌 Features

✔ Normal & Scroll-based progress modes  
✔ Custom colors  
✔ Label toggle  
✔ Adjustable height & border radius  
✔ Pure React — no dependencies except `react` & `react-dom`  
✔ Requires **no framework** inside Webflow  
✔ Works via HTML embeds using `data-*` attributes  

---

## 📁 Folder Structure

